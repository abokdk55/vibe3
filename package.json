{
  "name": "ahn-byungok-landing",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "24.x" },
  "scripts": {
    "dev": "npx vercel dev",
    "preview": "node scripts/preview.cjs",
    "check": "node scripts/check.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
