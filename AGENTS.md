# 프로젝트 작업 지침

이 문서는 저장소 전체에 적용되는 AI 코딩 에이전트의 작업 지침이다. 사용자와의 설명과 작업 결과는 한국어로 작성한다.

## 프로젝트 구조

- 프런트엔드는 별도 프레임워크나 번들러가 없는 HTML, CSS, JavaScript이다.
- `index.html`, `site.js`: 강의 안내 홈페이지, 공개 콘텐츠 API 연결, 상담 신청과 상세 페이지 콘텐츠 갱신.
- `style.css`, `curriculum/*.html`: 강의 상세 페이지의 공통 스타일과 한국어 콘텐츠.
- `admin/index.html`, `admin/admin.css`, `admin/admin.js`: 관리자 로그인, 문의 관리, 콘텐츠 편집 화면.
- `api/*.js`: Vercel Node.js 서버리스 API. CommonJS의 `require`와 `module.exports`를 사용한다.
- `api/_lib/auth.js`: 세션 쿠키 서명 및 비밀번호 해싱.
- `api/_lib/data.js`: 기본 콘텐츠와 비공개 Vercel Blob 데이터 접근.
- `vercel.json`: `cleanUrls: true`, `trailingSlash: false`. 페이지 링크 변경 시 실제 배포 경로와 리다이렉트를 확인한다.

## 실행 환경과 명령

- npm을 사용하고 `package-lock.json`을 유지한다. 의존성 설치는 `npm ci`를 기본으로 한다.
- `package.json`의 Node.js 버전은 `24.x`이다. Vercel 프로젝트의 런타임 버전도 맞춘다.
- API를 포함한 로컬 개발은 Vercel CLI의 `npx vercel dev`를 사용한다. CLI 설치나 프로젝트 연결이 필요할 수 있다.
- 단순 정적 파일 서버에서는 `/api/*`가 실행되지 않는다. 정적 화면 확인과 API 동작 확인을 구분한다.
- `npm run dev`: Vercel API 포함 개발 서버. `npm run preview`: 비밀값과 API를 읽지 않는 정적 미리보기.
- `npm run check`, `npm test`, `npm run test:browser`로 정적·모의 API·브라우저 검증을 수행한다. 별도 빌드·린트 스크립트는 없다. PowerShell에서 필요하면 `npm.cmd`를 사용한다.

## 필수 환경 변수

로컬에서는 Git에서 제외되는 `.env.local`, 배포에서는 Vercel 프로젝트의 해당 환경에 설정한다. 아래에는 변수 이름과 용도만 기록하며 실제 값은 문서, 소스 코드, 로그 또는 응답에 노출하지 않는다.

| 변수 | 용도 |
| --- | --- |
| `BLOB_READ_WRITE_TOKEN` | 비공개 Vercel Blob 저장소 읽기·쓰기 |
| `SESSION_SECRET` | 32자 이상 무작위 세션 서명용 비밀값 |
| `ADMIN_PASSWORD` | 인증 Blob이 없을 때만 사용하는 초기 비밀번호, 12~128자 |
| `RECOVERY_CODE` | 32~256자 무작위 비밀번호 복구용 비밀값 |

- 개발·검증용 Blob과 비밀값은 운영 환경과 분리한다. 연결 대상을 확인하기 전에는 저장, 문의 등록, 상태 변경, 비밀번호 재설정 등 쓰기 요청을 실행하지 않는다.
- 비밀번호 재설정 후에는 Blob에 저장된 해시가 `ADMIN_PASSWORD`보다 우선한다.
- 세션 쿠키는 `HttpOnly`, `Secure`, `SameSite=Lax`를 사용한다. 인증 검증 시 HTTPS 및 브라우저의 쿠키 전달 여부를 확인하고, 로컬 문제를 해결하기 위해 운영 쿠키 보호 속성을 제거하지 않는다.
- 실제 `.env*`, `node_modules/`, `.vercel/`과 인증 정보는 커밋하지 않는다. 빈 값만 있는 `.env.example`은 명시적 Git 예외이며 변수 변경 시 함께 갱신한다.

## 구현 규칙

- 먼저 `git status --short`와 관련 파일을 확인한다. 사용자의 기존 변경 사항을 보존하고 요청 범위에 필요한 수정만 수행한다.
- 기존 순수 JavaScript 구조를 따른다. 작업에 필요하지 않은 프레임워크, 빌드 도구, 대규모 리팩터링을 도입하지 않는다.
- 수정 파일의 들여쓰기와 명명 규칙을 따른다. 서버 코드를 브라우저 코드에 포함하지 않는다.
- 파일은 UTF-8로 읽고 저장한다. PowerShell에서 한국어 파일을 읽을 때 `Get-Content -Encoding UTF8`을 사용한다. 터미널 출력 깨짐만으로 원본을 일괄 수정하지 않는다.
- 한국어 파일명과 기존 URL을 보존한다. 강의 변경 시 `DEFAULT_CONTENT.curriculum`, 관리자 편집 필드, 해당 상세 HTML의 일치 여부를 확인한다.
- UI 변경 시 모바일 레이아웃, 키보드 접근, 입력 필드 레이블, 오류 메시지를 확인한다. 사용자 입력은 기본적으로 `textContent`로 표시하고, HTML 렌더링이 필요하면 허용 범위를 제한한다.

## API와 데이터 보호

- 콘텐츠 조회(`GET /api/content`)와 문의 제출(`POST /api/inquiries`)은 공개 기능이다. 문의 조회·상태 변경과 콘텐츠 저장에는 관리자 인증을 유지한다.
- 로그인과 비밀번호 재설정의 자격 검증, 비밀번호 해싱, 세션 서명을 우회하거나 약화하지 않는다.
- 입력 형식·길이·허용값을 서버에서 검증하고, 관리자 화면과 API의 요청·응답 형식을 함께 확인한다. 문의 상태는 현재 UI의 `new`, `contacted`, `done`과 호환되게 유지한다.
- 문의의 스팸 방지용 `website` 필드와 필수값 검증을 보존한다. 문의 연락처와 메시지 등 개인정보를 로그에 남기지 않는다.
- Blob 경로는 `data/content.json`, `data/inquiries.json`, `data/admin-auth.json`이며 모두 비공개다. 저장 경로나 데이터 형식을 바꿀 때 기존 데이터 호환성을 고려한다.
- Blob 읽기는 `useCache: false`를 유지하고, 데이터 없음만 기본값으로 처리한다. 통신 오류·인증 오류·손상 데이터를 기본값으로 숨기지 않는다.
- Blob 갱신은 ETag 조건부 쓰기와 제한된 충돌 재시도를 사용한다. 문의 추가·상태 갱신은 최신 목록과 합치며 무조건 덮어쓰지 않는다. API 오류는 공통 처리기로 안전한 JSON 응답을 보낸다.
- 관리자 API의 `isAuthenticated`는 비동기 함수다. 반드시 `await`하고, 비밀번호 변경에 따른 세션 무효화를 유지한다.
- `api/_lib/rate-limit.js`는 비공개 `data/rate-limits/` 버킷에 HMAC 식별자로 제한 상태를 공유한다. 테스트에서는 반드시 메모리 저장소를 사용한다.
- 운영 데이터 초기화·덮어쓰기, 비밀번호 변경, 배포와 원격 푸시는 사용자 요청 범위에 포함된 경우에만 수행한다.

## 변경 후 검증

- 문서만 변경한 경우 내용과 `git diff --check`를 확인한다.
- JavaScript 변경 시 변경 파일별로 `node --check <파일경로>`를 실행한다. 이는 문법 검사이며 동작 테스트를 대체하지 않는다.
- 화면 변경 시 관련 페이지의 데스크톱·모바일 표시, 링크, 폼, 브라우저 콘솔 오류를 확인한다. `tests/browser/`는 API를 가로채므로 운영 데이터에 접속하지 않는다.
- 인증·API 변경 시 분리된 개발 환경에서 정상 요청, 인증 실패, 잘못된 입력, 저장 후 재조회 등 변경과 관련된 경로를 검증한다. 외부 저장소 없이 테스트할 때는 모의 데이터 접근을 사용한다.
- Node.js 테스트와 Playwright가 구성되어 있다. 단순 문구 수정에 불필요한 테스트를 추가하지 않고, 인증·데이터 로직 변경에는 회귀를 잡을 수 있는 검증을 추가한다. 실제 Blob·Vercel 검증을 모의 테스트와 구분한다.
- 작업 완료 시 변경 내용, 실제 수행한 검증, 미검증 항목과 이유를 간결하게 보고한다. 환경 변수나 도구가 없어 실행하지 못한 검증을 성공으로 보고하지 않는다.
